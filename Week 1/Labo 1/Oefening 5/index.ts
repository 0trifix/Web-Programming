import {questionFloat} from "readline-sync";

let bedrag:number = questionFloat("Geef het bedrag in: ")
let soort_geld:number[] = [500, 200, 100, 50, 20, 10, 5, 1, 0.5, 0.2, 0.1, 0.05, 0.02, 0.01]

for (let index = 0; index < soort_geld.length; index++) {
  const element = soort_geld[index];
  
}
